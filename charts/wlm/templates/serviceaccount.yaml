{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "wlm.serviceAccountName" . }}
  labels:
    {{- include "wlm.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
secrets:
  - name: {{ include "wlm.serviceAccountName" . }}-token
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wlm.serviceAccountName" . }}-token
  annotations:
    kubernetes.io/service-account.name: {{ include "wlm.serviceAccountName" . }}
type: kubernetes.io/service-account-token
{{- end }}

