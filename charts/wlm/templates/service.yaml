apiVersion: v1
kind: Service
metadata:
  name: {{ include "wlm.fullname" . }}
  labels:
    {{- include "wlm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - nodePort: {{ .Values.service.nodePort }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "wlm.selectorLabels" . | nindent 4 }}

