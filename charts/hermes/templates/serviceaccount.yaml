{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hermes.serviceAccountName" . }}
  labels:
    {{- include "hermes.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
secrets:
  - name: {{ include "hermes.serviceAccountName" . }}-token
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hermes.serviceAccountName" . }}-token
  annotations:
    kubernetes.io/service-account.name: {{ include "hermes.serviceAccountName" . }}
type: kubernetes.io/service-account-token
{{- end }}