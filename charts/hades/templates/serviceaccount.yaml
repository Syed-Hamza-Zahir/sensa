{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hades.serviceAccountName" . }}
  labels:
    {{- include "hades.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
secrets:
  - name: {{ include "hades.serviceAccountName" . }}-token
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hades.serviceAccountName" . }}-token
  annotations:
    kubernetes.io/service-account.name: {{ include "hades.serviceAccountName" . }}
type: kubernetes.io/service-account-token
{{- end }}
